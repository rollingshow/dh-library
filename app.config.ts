export default defineAppConfig({
    // Конфигурация поиска
    searchAmountDefault: 12,
    // Провайдеры для обложек книг
    bookCoverProviders: {
        openLibrary: true,
        googleBooks: true,
        apeBooks: true
    }
})
